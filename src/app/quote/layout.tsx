import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Get a Quote — PearlGate | Free Sourcing Consultation",
  description: "Tell us what you need. We'll match you with verified Guangdong factories, provide quotes within 48 hours, and handle all communication in Chinese.",
};

export default function QuoteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
